# CAD 정본 · 명명법 · 변경 로그 (frame generation 5)

> **이 문서는 `cad/` 폴더의 단일 정본(single source of truth)입니다.**
> 팀원과 (다른 대화창의) Claude 모두, 작업 전 여기서 **① 현재 구성 ② 명명법 ③ 이력**을 먼저 확인하세요.
> 핵심 구조: **지오메트리는 전부 `frame_master.scad` 하나에** 있고, 각 파트 파일은 그 마스터를 `include`하고 `part_mode`만 지정하는 **얇은 선택자**입니다. 그래서 "치수를 바꾼다 = `frame_master.scad`를 바꾼다"입니다.

---

## 1. 현재 최종 구성 (frame generation 5)

로봇 본체 '어름이'. **알루미늄 프레임(2020)·기성품(FR12 힙·608 베어링·XM430·AS5047P 42mm 모듈)은 출력물이 아님.**

### 마스터
| 파일 | 역할 |
|---|---|
| `frame_master.scad` | **공용 지오메트리 정본** (구 `frame_v5.scad`, 현재 상태 = 아래 v5.4). 모든 파트가 이 파일을 include. |

### 하체 (lower)
| 파일 | 소분류 | 역할 | 출력수 |
|---|---|---|---|
| `lower_spine_v5.1.scad` | spine | 하체 스파인(힙↔발목 골격), 힙 +9 반영 | 1 |
| `lower_ankle_carrier_v5.2.scad` | ankle | 개방형 발목 캐리어(608 하우징 2 + 상승 브리지 + 엔드월). **v5.2: 랜드 보어 d19 관통 + 거싯 클립(칼라 간섭 제거)** | 1 |
| `lower_ankle_collar_v5.1.scad` | ankle | 발목 안쪽 락칼라(노즈 연장형), 내경 7.9 | **2** |

### 상체 (upper) — **A/B 구성 (C는 미진행)**
| 파일 | 소분류 | 역할 | 출력수 |
|---|---|---|---|
| `upper_bodyA_v5.1.scad` | bodyA | 상체 A(스플라이스 리시버, 하단 캡→테이퍼) | 1 |
| `upper_bodyB_v5.4.scad` | bodyB | 상체 B(장비 베이). **v5.4: 선반 아래 베이 3면 완전 개방(케이블면 최대 개방 — X- 벽 + 양쪽 Y벽 전면 스트립 제거, 격벽+후면 스트립+X+ 벽 = 닫힌 후방 토션셀). 배터리 존은 v5.3 전폭 개구(93) 유지, 테이프+안착턱·슬리브·+22mm 유지** | 1 |

### 축 (axle)
| 파일 | 소분류 | 역할 | 출력수 |
|---|---|---|---|
| `axle_ankle_socket_v5.1.scad` | ankle | 발목 하단 소켓(축 일체, 자석 포켓 팁) front/rear | 2 |
| `axle_pivot_front_v5.1.scad` | pivot | 상단 피벗 전방 소켓(짧은 축) | 1 |
| `axle_pivot_rear_v5.1.scad` | pivot | 상단 피벗 후방 소켓(짧은 축 + 자석 포켓) | 1 |
| `axle_pivot_collar_v5.1.scad` | pivot | 상단 피벗 락칼라, 내경 7.9 | **2** |

### 엔코더 (encoder)
| 파일 | 소분류 | 역할 | 출력수 |
|---|---|---|---|
| `encoder_ankle_bracket_v5.1.scad` | ankle | 발목 엔코더 L-브래킷(42×42 보드, 4×M3, 8mm 스탠드오프) | 1 |
| `encoder_pivot_box_v5.1.scad` | pivot | 피벗 엔코더 박스(42×42, 4×M3, 8mm 스탠드오프) | 1 |
| `encoder_pivot_bearing_v5.1.scad` | pivot | 피벗 전방 베어링 마운트(구조물) | 1 |

### 유틸 / 폐기 / 기타
| 경로 | 내용 |
|---|---|
| `util_ankle_section_view.scad` | 발목 조립 단면 검사 뷰(**비출력**, 버전 없음) |
| `deprecated/` | **폐기 파트**(출력 금지) — `ankle_pcb_lid`(개방형 브래킷으로 대체), `upper_body_door`(A/B 구성, 도어 없음), `solid_coupler`·`oldham_coupler`(v5 은퇴, 중복) |
| `archive/` | 구세대 마스터 `frame_v1~v4.scad`(동결 보관) |
| `exports/` | 슬라이서 산출물(`.3mf`/`.gcode.3mf`) — 소스 아님 |

---

## 2. 명명법 (naming convention)

**형식:** `<대분류>_<소분류>_v<세대>.<파트리비전>.scad`

- **대분류(5종):** `frame` · `lower`(하체) · `upper`(상체) · `axle`(축) · `encoder`(엔코더 관련)
- **소분류:** 위치+부품(예: `ankle_carrier`, `pivot_rear`, `bodyA`) — 어느 파트인지 즉시 식별.
- **버전 `v<세대>.<리비전>`:**
  - **세대(앞자리)** = 프레임 세대. 현재 전부 **5**. **구조 대개편 때만** 6으로 올리고, 그때 각 파트 리비전은 `.1`로 리셋.
  - **리비전(뒷자리)** = 그 파트 지오메트리가 바뀔 때마다 +1. 이번 명명 정리 시점을 리셋 기준으로 **현행 전 파트를 `v5.1`**로 둠(이전 이력은 아래 3장 LOG).
- **마스터:** `frame_master.scad` (이름에 버전 없음). 실제 상태는 이 CHANGELOG 최신 LOG로 관리 → **항상 현행 마스터 1개**라 최신 혼동 없음.
- **원칙:** **파트당 현행 파일은 정확히 1개.** 그래서 "어느 게 최신"이 항상 자명함.
- **폐기 →** `deprecated/`, 파일명 `_DEPRECATED`, 헤더에 대체 파트 명시. **아카이브 →** `archive/`. **산출물 →** `exports/`.

### ⚙ 다른 Claude 세션 / 팀원 작업 절차 (필독)
1. **치수·형상 변경은 `frame_master.scad`를 편집**(파트 래퍼는 include만 하는 선택자라 보통 안 건드림).
2. 어떤 **파트의 지오메트리가 바뀌면**, 그 파트 파일을 **리비전 +1**로 rename(`_v5.1` → `_v5.2`), 헤더/이 CHANGELOG에 로그 추가.
3. 새 파트는 `<대분류>_<소분류>_v5.1.scad`로 만들고 `include <frame_master.scad>` + `part_mode` 지정.
4. 폐기 시 `deprecated/`로 이동 + `_DEPRECATED` + 대체 파트 명시.
5. 반드시 **브랜치 → PR**로 올리고, OpenSCAD 렌더로 매니폴드/조립 경고 0 확인 후 병합.
6. 커밋/푸시는 HTTPS+토큰(파인그레인드, 이 리포 한정). SSH 원격은 이 환경에서 불가.

---

## 3. 세부 변경 로그 (LOG, 최신 우선)

### v5.7 — 2026-07-25 (새벽) · 상체 B 베이 3면 완전 개방: `upper_bodyB` v5.3→v5.4 (같은 요청의 병렬 작업 병합)
**배경:** 아래 v5.6(립 제거)과 이 v5.7은 **같은 사용자 요청("OpenCR 케이블면 최대 개방")을 두 세션이 병렬 작업**한 결과 — v5.6이 먼저 main에 올라가 v5.3을 선점했고, 이 작업은 v5.4로 리비전 +1 하여 병합. v5.3(개구 85/93 전폭 X- 개방)만으로는 Y벽이 보드(75) 측면 에지에서 편측 5mm에 있어, **면 방향(에지) 커넥터 — 파워 케이블, micro-USB, 스위치 — 의 측방(Y±) 직선 플러그 삽입 경로가 여전히 막힘** → 한 단계 더 개방.
- **선반 아래 베이 3면 완전 개방:** 격벽 전면(`bulk_x0`, 테이프 패드면) **앞쪽(x < bulk_x0)의 모든 재료 제거** — X- 벽에 더해 **양쪽 Y벽 전면 스트립까지**, 플레어 용접 상단(z 453)부터 배터리 선반 하면(z 587)까지 전 폭. 보드 구역이 X-·Y-·Y+ 3면 개방되어 모든 에지 커넥터가 **직선 삽입 경로** 확보. 선반 위 배터리 존은 v5.3 전폭 X- 개구(93, 립 없음) 유지.
- **구조 보상:** 격벽(양 Y벽에 용접) + Y벽 후면 스트립(x 1.1~19) + X+ 벽 = 베이 전 높이에 걸친 **닫힌 후방 토션셀** — v5.6의 "주 보강은 전진 격벽" 판단을 형상으로 확정. 안착턱(6×60)은 격벽면 앞으로 돌출하는 유일한 예외로 유지(difference 후 재합집합으로 재구성).
- **불변:** 전체 높이(robot_l2 372, B 파트 ~221mm ≤ X1C 256), `ocr_center_z`/ELL_IMU, 테이프 패드+안착턱 마운트, 닫힌 텔레스코핑 슬리브, 선반 전면 토글 슬롯(*), 보드 상단 케이블 여유 25mm, 배터리 존·충전창.
- **파일:** `upper_bodyB_v5.3.scad` → **`upper_bodyB_v5.4.scad`** rename(+헤더 갱신). 지오메트리는 전부 `frame_master.scad`에서 수정. 어셈블리는 변경 불요(동일 모듈 호출).
- **검증:** 파트 렌더 매니폴드 **Simple: yes**·경고/에러 0(STL watertight), 전체 조립 렌더 Simple: yes·경고 0. 단면 검사: 보드 중앙높이에서 x∈[1.1,19]만 잔존(격벽면 앞 재료 0), 안착턱 돌출(-4.9) 정상, 배터리 존 X- 개구 정상.

### v5.6 — 2026-07-25 · 상체 B X- 서비스면 완전 개방 (v5.2→v5.3)
**계기(사용자):** OpenCR는 보드 법선(X-) 방향뿐 아니라 보드 면방향(가장자리의 전원 리드·USB·핀헤더, ±Y/±Z로 돌출)으로도 케이블이 연결됨 → 전면 개구를 최대로 열어 엣지 케이블이 앞으로 꺾여 나올 공간과 삽입·결선 접근성 확보.
- **리턴 립 제거(lip_ret_y 3→0):** 개구가 내부 전폭으로 확장 — 90존 **79→85**(보드 75 대비 편측 5), 헤드존 **87→93**. 컷 범위 z 453(플레어 상단)~627(캡 하단) 불변.
- **강성 대체 경로(문서화):** 립의 에지 보강 역할은 전진 격벽(양 Y벽을 보드 전 높이로 잇는 웹)·닫힌 X+ 벽·하부 플레어 용접·상부 헤드/캡이 담당(06 로그 "주 보강은 전진시킨 격벽"과 일치). 플레어 구간(445~453) X- 벽은 유지(용접 구조).
- 검증: STL 레이 계측으로 개구 폭(±42/±45 개방, 벽 43/46.5~ 온전)·플레어 폐구간 확인, 파트·전체 조립 렌더 Simple:yes·경고/에러 0. 높이 221 불변.
- ※ 파트 파일은 part_mode 선택자(얇은 래퍼) — 지오메트리는 frame_master.scad에 있음(1장 아키텍처).

### v5.5 — 2026-07-24 (저녁) · 발목 캐리어 간섭 수정 + 상체 B 확정형(A/B, 도어 없음) + 피벗 검증
**① 피벗 축높이 검증(변경 없음):** `encoder_pivot_box`와 반대편 `encoder_pivot_bearing` 모두 **베어링 중심축 = 장착면(프로파일 상면) +25.0mm**로 STL 계측 일치 확인(둘 다 `axis_z`/`mount_rise` 공유 파라미터로 유도 → 구조적으로 동기). 수정 불필요.
**② 피벗 박스 PCB 홀 관통 확인+보강:** 후면 판(5t)의 4×M3(3.4) 홀은 레이/단면 검사상 **이미 완전 관통**이었음(막힌 것처럼 보였다면 프리뷰(F5) 아티팩트 가능성). 컷 실린더를 판 앞뒤 **각 2mm 오버슛**(t+0.4→t+4)으로 키워 향후 판 위치/두께 변경에도 확실히 뚫리도록 보강 — 출력 지오메트리 불변이라 리비전 유지(v5.1).
**③ `lower_ankle_carrier` v5.1→v5.2 (간섭 2건 수정):**
- **랜드 보어 관통:** 구 컷(`2*ring_face−brg_w`)이 |y|29.85에서 멈춰 랜드월(31..33)에 8.4 축공만 남음 → 11.4 노즈가 통과 못 하고 **칼라가 엔드월 면을 눌러 마찰**. 컷을 링 페이스까지 연장, **랜드 보어 d19 완전 관통**(608 내륜 OD 12 < 19 < 외경 22 → 노즈는 내륜만 접촉, 랜드는 외륜만 지지).
- **거싯 클립:** 거싯 헐이 하우징 내측면(|y|=31)을 넘어 |y|=29.6까지 개방 중앙으로 돌출 → 회전 칼라 스윕(최대 반경 12.8, |y|24.4..30.4)과 간섭. 각 거싯을 **|y|=31 평면에서 클립** — 엔드월 면 밖으로 아무것도 돌출하지 않음.
- **검증:** 칼라 회전 스윕 솔리드(몸통 r12.82 + 노즈 r5.7) ∩ 캐리어 = **공집합**(수정 전 176mm³). 노즈-보어 반경 여유 3.8, 칼라 몸통 외측면↔랜드월 내측면 0.6, 러그 상단(12.3~12.8)↔엔드월 하단(z14) ≈1.2~1.7. 매니폴드/경고 0.
**④ `upper_bodyB` v5.1→v5.2 (확정형 A/B, 도어 없음, +22mm):**
- **높이 +22 (`robot_l2` 350→372):** 선반이 보드 상단을 3mm 위에서 덮고 있어 추가 케이블 연결 불가 → 선반/헤드만 +22 상향, **보드 상단 개방 여유 3→25mm**. 보드 자체는 그대로(ocr_center_z 불변 → **ELL_IMU 250.5 유지**). **B 파트 높이 221mm ≤ X1C 256** ✓.
- **오픈 X- 페이스:** 도어(나사 6·보스·리베이트) 전부 삭제, X- 벽 제거 + 리턴 립 3(개구 90존 79 / 헤드존 87). `upper_body_door` part_mode는 폐기 echo만.
- **OpenCR = 테이프 패드 + 하단 안착턱:** 스냅 리벳(튜브·푸시홀·핀홀) 은퇴. 격벽 전면 = 보드 배면(테이핑 패드, bulk_x0=ocr_back_x), 보드 하단은 안착턱(6×60)에 안착.
- **A-B 스플라이스 = 닫힌 텔레스코핑 슬리브**(벽 2.5, 편측 유격 0.3, split 위 8mm 플레어 용접): X-텅 파손 문제 해결. 크로스볼트 2 동일, A는 무변경.
- **전원 토글 = 선반 전면개방 슬롯**(폭 13·깊이 13, y=−20, 전부 (*) 실측 후 조정).
- **여유 변화(에코):** 매달림 바닥 여유 132.2→**110.2mm**(안전). ⚠**강체 스윕 여유 6.5→−15.5mm**: 매달림 강체 난폭 스윙은 이제 **|φ|<18.4°에서만 무충돌**(충돌창 ±[18.4°,19.8°], 에코가 자동 산출). 직립 밸런싱은 무영향. 여유 복원이 필요하면 B 높이 재축소 필요(트레이드오프 결정 사항).
- **어셈블리(master) 상체 갱신:** `robot_assembly`에서 도어 호출 삭제 → 마스터 조립이 현행 A/B 상체를 표시.
- ★후속: 시뮬/FW `L2 0.350→0.372` 갱신 + 질량 재실측(질량비·λ 재확인), 토글/충전창 실측.
- 검증: 전 수정 파트 + 조립 렌더 매니폴드 Simple:yes·경고/에러 0.

### v5.4 — 2026-07-24 · 42mm 모듈 실측 반영 + 명명법 전면 개편
- **엔코더 홀 실측:** 4코너 그리드 **37→34(코너 4mm 안쪽 = ±17)**, **M3 셀프탭(2.7)→M3 관통(3.4)**. 나사가 브래킷을 지나 모듈 뒷면 기둥 끝에 체결되므로 관통이 맞음. (발목·피벗 공통, `as_hole_dx/dy`, `as_hole_d`)
- **뒷면 8mm 기둥 대응(`as_back_post=8`):** PCB 뒷면 r3·8mm 기둥 4개를 고려해 장착판을 8mm 뒤로(발목 브래킷 `plate_y0`/`akc_brk_bar_y0`, 피벗 `enc_plate_in_y`). **칩↔자석 공극 1.5 불변.**
- **락칼라 내경:** `shaft_d+0.25`(8.25) → `shaft_print_d+0.1`(**7.9**). 실제 7.8 축에 헐거움(0.45) 해소 → 스냅핏. (발목·피벗 칼라 공통)
- **상체 확정:** **A/B 구성 유지**(ABC 분할·battery lid `upper_body_c` 미진행, 도어 폐기).
- **명명법 개편:** `frame_v5.scad`→`frame_master.scad`, 전 파트 `<대분류>_<소분류>_v5.1.scad`로 rename, 폐기 4종 `deprecated/`, 구프레임 `archive/`, 산출물 `exports/`. include 전부 `frame_master.scad`로 정리(구 v3/v4 include 버그 동시 해소). 전 파트 렌더 매니폴드·조립 경고 0 검증.
- 검증: 발목·피벗 공극 1.5, 전 현행 파트 단일 매니폴드, assembly 경고 0.

### v5.3 — 2026-07-19 (저녁~) · 42mm 실물 모듈 대응(설계)
- 실물 = 쿠팡 하이제니스 "AS5047P 14bit SPI 42mm"(자석 포함). 42×42 보드 대응으로 대개편.
- **발목:** 보드 상단 z=+21 → **브리지 z14→z23**, **플랜지 z0 22→31(+9)**, 하체 통째 +9(`robot_l1 250→259`). 상승 브리지를 하우징에 잇는 **엔드월** 추가. 브래킷 T→**L(46폭, 4코너 홀)**.
- **피벗:** 42 보드가 기존 창에 안 들어가 프레임 **비대칭화**(하면 프로파일 고정, 상단바 축+35). 보드 커넥터-UP 장착. 구 2×M2+슬롯 → 4코너 그리드.
- (당시 홀 그리드는 37 **추정**, 셀프탭 가정 — v5.4에서 실측 34·관통으로 정정)

### v5.2 — 2026-07-19 (낮) · 개방형 캐리어
- 닫힌 캐리어+바닥 리드 은퇴 → **개방형 캐리어**(베어링 하우징 2 + 상부 브리지). 중앙 개방으로 자석·PCB·칼라를 열린 중앙에서 조립.
- **안쪽 락칼라 2개 부활**(노즈 연장형). PCB는 **T-브래킷**으로 브리지 하부에 장착. `ankle_pcb_lid` 폐기.

### v5 / v5.1 — 2026-07-19 · AS5047P 엔코더 전면 도입
- AN25+커플러 은퇴, **AS5047P 비접촉 엔코더**로 교체(피벗·발목). 후방 축 **48→13** 단축 + 축단 자석 포켓. 발목 하단 소켓 **축 일체 출력**. `solid_coupler`/`oldham_coupler` 은퇴.

### v4 — 2026-07-14 · 상체/힙 확정
- 상체 A/B/door, 머리 98·몸 90·발 80, FR12 힙 브래킷, 배터리 선반. (상세는 archive/06 조립피팅 로그)

### v3 — 2026-07-13 · 로봇 본체 최초
- 프레임+줄만 있던 상태에 로봇 본체 최초 추가(상체40/하체25 초기값). 피벗 마운트 상면 장착.

---
*정본 규칙: 값·구조 판단이 필요할 땐 이 CHANGELOG의 최신 LOG를 우선한다. 파일이 여러 개로 보이면 반드시 `_v5.<최댓값>` 또는 `frame_master.scad`가 현행이다.*
