// Standalone v5.2 ankle INNER lock collar - PRINT 2 (one per side).
// Extended-nose collar that clamps the printed socket axle just inboard
// of the 608 inner ring: hub stop | inner ring | this collar = positive
// axial retention both ways (glue optional).
// Clamp bolt: M3 through the split lugs, reachable from the open center
// of the v5.2 carrier. Nose length bridges the housing land so the collar
// body clears the housing inner wall.
// Edit shared dimensions in frame_v5.scad so everything stays linked.

part_mode = "ankle_collar";
print_export = true;

include <frame_master.scad>
