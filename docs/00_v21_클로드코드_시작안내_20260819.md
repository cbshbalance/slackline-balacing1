# 00. v21 개발 — Claude Code 시작 안내 (2026-08-19)

> 이 폴더(docs/)의 v21 관련 문서는 claude.ai 프로젝트 「줄타기」에서 **v21 개발에 필요한 것만 추린 사본**이다
> (2026-08-19 기준). 정본은 claude.ai 프로젝트이며, 그쪽이 갱신되면 사본도 다시 내려받아야 한다.

## 읽는 순서 (코드 작업 전)

1. **55** — v21 개발 인계 (기본 맥락. 단 §3-4 예측값·교육 시나리오는 이후 문서로 일부 대체됨)
2. **56** — 개발 계획 v2 (기능 명세 · 수용 기준 · 작업 규칙)
3. **58** — 야간 개발 완료 보고 (0~3단계 구현 완료 + 구현 설계 요점 + 개발 중 밟은 함정)
4. **59** — 영상 시퀀스·스파이크 정정 (**최신 상태.** "참값 부호반전"은 버그 아님 — 접기 β̇ 킥의 물리, 표시 옵션으로 정리됨)
5. **57** — P2R 실측 0.417 / **53** — λ 실측 5.3 (T₂ 130 ms) / **54** — Â 잡음 바닥 (실측 정본 3종)
6. **46** — 실측 세 개 (이론→실기 다리, 함정 목록) / **50** — 놓기 프로토콜 (v21 집기 기능의 실물 대응)
7. 씬 체인 작업 시에만: **49** — 발표 서사 3단계 / **51** — 3분 대본
   ⚠ 49·51은 **미확정** — 학생 토의로 계속 수정 중. 씬 체인은 순서 교체가 코드 수정 없이 되는 **배열 데이터**로 만들 것.

## 코드

- 작업 대상은 **v21_pres/ 만**. v19_state·v19_sim 등 원본 폴더 불변 (문서 55 §6 규칙 — v21_pres/CLAUDE.md 참조).
- `v21_pres/README_v21.md` = 기능·서버 명령·시험 요약.
- 레포의 `tmp/`(1920 파일), `work/`, `v1_*`~`v18_*`, `cad/`, `exp_data/` 는 v21 과 무관 — **읽지 말 것**.
- `docs/` 의 나머지 연구 문서도 위 목록 밖이면 기본적으로 읽을 필요 없다.
- 시험: `cd v21_pres && python tests_v21.py` (헤드리스 수용시험 — 수정 후 반드시 통과 확인).

## 남은 일 (문서 58 §4 우선순위)

1. **4단계**: 실측선 오버레이 입력칸 (r·절편·λ 입력, 기본 λ=5.3 → 별색 선·띠 + w=[−1/r,1,−1/(rλ),1/λ] 병기)
   / 실측 발목잡음 프리셋 (sim_engine `_measure` 채널별 σ 확장, 기본 0.223° — 베어링 교체 후 재측정값으로 갱신)
2. **씬 체인 배열 데이터화** (SCENES 배열 + `[` `]` 키 — 49·51은 참고 초안)
3. 로봇 실기 영상 오버랩 (레이어 시스템은 완성 — 소재만 넣으면 됨)
4. 실측 ② (안정모드선 r·절편) 수행 시 → 실측선 오버레이에 기입
