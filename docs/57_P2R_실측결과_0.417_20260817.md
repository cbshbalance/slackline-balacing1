# 57. P2R 실측 결과 — 0.417 (2026-08-17, 사후 기록)

> 문서 46 「실측 ① — P2R」의 결과. 측정은 별도 세션에서 수행되었고 그 대화에만 남아
> 문서화가 누락되어 여기 사후 기록한다 (사용자 보고, 2026-08-17). 원 로그·적합표는 확보되는 대로 보강.

## 결과

- **P2R = 0.417** (α–δ 직선 기울기 −0.417) → P1R = 1 − P2R = **0.583**
- 예측(질량표 유도, 문서 46) −0.4122 대비 **+1.2%**

## 판정

- 실측 ①의 목적 달성 — 엔코더 값에서 β = P1R·α + P2R·θ 를 **실측 비율로** 만들 수 있다.
- 질량표(7/30~31 저울·균형점 실측)에서 유도한 값과 독립 방법으로 1.2% 일치 — 모델의 질량·CoM 계열이 교차검증됨.
- 1.2% 차이는 A 위치항에 β 스케일로 들어가는 수준이라 문턱 0.6° 대비 실용 무차별. 방침: **펌웨어·실측 w 계산에는 실측 0.417 을 정본으로**, 시뮬 내부는 질량표 세트 유지(자기일관성).

## 보강할 것

- 원 로그/적합 표 (상행·하행 평균 여부, 점 개수, R²) — 이 문서에 추가.
- **절편** — 문서 52 §4의 영점 절차에 따라 해석이 갈린다 (매달아 z = 참영점·절편 구조적 0 / 기계 기준면 z = 절편이 곧 영점 오차). 어느 절차로 쟀는지 기록 필요.
- w 재계산 (문서 54 조치 5, λ 실측 5.3 반영) 시 이 값 사용.
