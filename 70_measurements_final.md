# 70. 실측 세 개 — 최종 정리 (2026-08-22)

> 문서 46 「증분접기에 필요한 실측 세 개」가 요구한 세 값이 모두 측정됐다.
> 이 문서가 그 **정본**이며, 문서 64·65·69 를 대체한다.
>
> **참고 범위:** 문서 46 이후 문서만 근거로 쓴다. 그 이전 문서에서는 측정 방법만 가져온다.

---

## 1. 실측 상수 — 정본

| 상수 | **실측값** | 불확실도 | 출처 로그 | 상태 |
|---|---|---|---|---|
| **P2R** | **0.4285** | ± 0.0011 (0.3 %) | `0822_p2r_test` | ✅ 확정 |
| **λ** | **5.44 /s** (T₂ = 127 ms) | ± 0.59 (11 %) | `0822_lambda_test` | ✅ 확정 |
| **r** | **−1.506** | ± 0.074 (5 %) | `0822_r_test` | ✅ 확정 |
| **c₀** | −1.11° | ± 0.86 (78 %) | `0822_r_test` | ⚠ **미확정** |

파생값:

```
P1R = 1 − P2R = 0.5715
β   = α + P2R·δ = α + 0.4285·δ
α   = ank − φ            ★ §2 — 펌웨어를 고쳐야 한다
w   = [−1/r, 1, −κ/(rλ), κ/λ] = [+0.664, 1, +0.217, +0.327]   κ = 1.83  (§7)
A 오프셋 = c₀/r = +0.73° ± 0.6°       ⚠ 오차가 크다
```

---

## 2. ★ 이 세션의 가장 중요한 발견 — α 공식이 틀려 있었다

| | |
|---|---|
| 문서 52 의 펌웨어 규약 | `α = ank + phi` |
| **올바른 것** | **`α = ank − phi`** |

`p2r_test.ino` 가 이 공식으로 `alpha_deg` 열을 만든다. **로그의 alpha 열이 틀린 값이고,
실기 펌웨어가 같은 규약이면 A 계산 전체가 틀린다.** 최우선 수정 대상이다.

### 이 한 줄이 바꾸는 것

| | `ank + φ` (구) | **`ank − φ` (정정)** |
|---|---|---|
| **r** | +0.749 (모델과 부호 반대) | **−1.506** (모델 −1.6445 와 8.4 %) |
| r 적합 R²(β̈) | 0.571 | **0.804** |
| **P2R** | 0.4328 ± 0.0027 | **0.4285 ± 0.0011** |
| P2R 적합 R² | 0.999719 | **0.999958** |
| P2R 히스테리시스 | 0.139° | **0.021°** |

**정정하면 두 실측이 동시에 좋아진다.** 특히 P2R 의 상행–하행 히스테리시스가
0.139° → 0.021° 로 줄어드는데, 이는 "발목 마찰" 로 해석했던 것의 대부분이
**마찰이 아니라 α 공식 오류였다**는 뜻이다.

λ 은 φ 하나로만 재므로 이 정정에 **영향받지 않는다.**

### 어떻게 찾았나

사용자가 "φ = +4 일 때 β ≈ −2 에서 놓았다" 고 기억했는데 로그는 `+5.3` 을 내놓았다.
α 정의를 바꾸자 같은 시행들의 β 가 −1.5 ~ −3.5 로 나와 기억과 일치했다.
**문서에 적힌 부호 규약을 검산 없이 받아쓴 것이 원인이다.**

---

## 3. 실측 ① — P2R (질량 배분비)

**방법** (문서 52 절차): 발목축에서 매달고 δ 를 0→10→20→30→40→30→20→10→0 으로 명령,
각 점에서 100 Hz 로 2 초(201 샘플) 평균. 매달린 평형은 β = 0 이므로 **α = −P2R·δ**.

| 점 | 실제 δ | α |
|---|---:|---:|
| 시작 | −0.178 | +0.253 |
| 상행 10 | 8.877 | −3.711 |
| 상행 20 | 18.264 | −7.643 |
| 상행 30 | 27.487 | −11.600 |
| 정점 40 | 36.643 | −15.604 |
| 하행 30 | 28.821 | −12.217 |
| 하행 20 | 19.161 | −8.071 |
| 하행 10 | 9.491 | −3.919 |
| 복귀 | −0.182 | +0.176 |

### 결과

```
P2R = 0.4285 ± 0.0011      P1R = 0.5715
절편 = +0.139°             R² = 0.999958
상행 0.4293 / 하행 0.4286  (차 0.16 %)
히스테리시스 폭 = 0.021°   (δ = 20 기준)
```

- **절편 +0.139°** — 매달아 `z` 를 눌렀으므로 구조적으로 0 이어야 하고, 실제로 0.14° 이내다.
- **히스테리시스 0.021°** — 발목 마찰은 사실상 없다. 상·하행 평균을 낼 필요조차 없다.
- **처짐** — 명령 40° 에 실제 36.64° (−3.36°). **P2R 은 명령각이 아니라 실제 δ 로 나눠야 한다.**
  명령각으로 적합하면 −6.8 % 틀린다.

---

## 4. 실측 ② — r (안정모드선) 과 c₀ (절편)

**방법:** 줄 위에서 놓기 64 회, 100 Hz. 자유비행 구간에서 (φ̈, β̈) 를 (φ, β, φ̇, β̇, 1) 로
회귀해 4×4 시스템행렬을 만들고, 불안정 실고유값의 좌고유벡터에서 r 을 얻는다.
잡는 순간(\|φ\| > 8°) 이후는 버린다.

### 4-1. 결과

```
r  = −1.506 ± 0.074        (모델 예측 −1.6445 와 8.4 %)
λ  = 5.66 ± 0.09           (실측 ③ 의 5.44 와 4 %)
c₀ = −1.11 ± 0.86          ⚠ 미확정
고유값 = +5.66,  −0.19 ± 5.32j,  −3.25
```

### 4-2. 세 경로가 같은 답을 낸다

| 경로 | 쓰는 것 | 결과 |
|---|---|---|
| ① 시스템 동정 | φ̈·β̈ 회귀 → 좌고유벡터 | **−1.506** |
| ② 놓기점 경계 | 놓은 자리 (φ, β) 와 낙하 방향만. **미분·모델 미사용** | **−1.494** (0.8 % 차) |
| ③ Ȧ = λA 시험 | A 가 불안정 좌표인지 직접 검사 | −1.5 ~ −2.0 에서 R² 0.88~0.89 |

②는 놓기점들이 **β = −0.670·φ** (상관 −0.976) 를 이룬다는 것이고, 1/기울기가 곧 r 이다.

### 4-3. 강건성

| 갈라보기 | λ | **r** |
|---|---:|---:|
| \|φ\| < 2.5 / 3 / 4 / 5° | 5.82 / 5.88 / 5.66 / 5.37 | −1.570 / −1.643 / −1.506 / −1.569 |
| 평활창 ±40 ~ ±130 ms | 5.54 ~ 5.62 | −1.380 ~ −1.514 |
| 놓은 뒤 250 / 500 / 1000 ms 만 | 3.97 / 5.55 / 5.64 | −1.650 / −1.527 / −1.500 |
| 홀수 / 짝수 시행 | 5.62 / 5.64 | −1.539 / −1.476 |
| 앞 절반 / 뒤 절반 | 5.57 / 5.73 | −1.368 / −1.591 |
| **잭나이프 (57 시행)** | **5.66 ± 0.09** | **−1.506 ± 0.074** |

### 4-4. 선형 영역은 \|φ\| < 5°

\|φ\| 상한을 6° 로 넓히면 불안정 고유값이 λ = 0.58 로 무너진다.
그 밖에서는 줄의 복원 토크가 이겨 **로봇이 넘어지는 대신 흔들린다.**
분석은 반드시 \|φ\| < 5° 표본만 쓴다. (놓는 각도 자체는 커도 된다 — 궤적이 그 구간을 지나가면 된다.)

---

## 5. 실측 ③ — λ (발산율)

**방법** (문서 53 보완): 놓기 19 회 중 유효 18 회. ψ = φ − φ_eq 의 ln\|ψ\| 대 t 직선적합,
밴드 \|ψ\| 2~9°, 상승부만. **관측 채널은 φ 하나** — β 는 진폭 대비 잡음이 커 쓸 수 없다.

### 결과

```
λ  = 5.44 ± 0.59 /s        배가시간 T₂ = 127 ± 14 ms
방향별: + 낙하 5.33 (n=9) / − 낙하 5.54 (n=9)  — 차 3.9 %
```

문서 53(4 회, T₂ = 127 ± 13 ms) 과 **자릿수까지 일치**. 회차를 4 → 18 로 늘려도 안 움직였다.

### ★ 평형은 φ = 0 이 아니라 **φ = +1.40°** 다

문서 53 의 방법을 그대로 쓰면 답이 낙하 방향에 따라 **1.9 배**로 갈린다 (+ 낙하 3.94 / − 낙하 7.53).
실제로 발산하는 것은 φ 가 아니라 **ψ = φ − φ_eq** 이기 때문이다.
φ_eq 를 훑어 두 방향이 일치하는 값을 찾으면 **1.40°** 이고, 그때 λ = 5.44 로 문서 53 과 만난다.

문서 53 이 이것을 못 본 이유는 **4 회를 전부 같은 방향으로 놓았기 때문**이다.
**회차를 늘린 것보다 방향을 섞은 것이 더 중요한 개선이었다.**

φ_eq 가 엔코더 영점 오차인지 진짜 기울어짐인지는 이 로그로 안 갈린다.
어느 쪽이든 **상태를 φ = 0 이 아니라 φ = +1.40° 기준으로 읽어야 한다.**

---

## 6. 부수 확정 사항

### 6-1. 발목 센서 수리 — 문서 54 의 목표 달성

| | 발목 고주파 rms | φ |
|---|---|---|
| 문서 54 (수리 전, 8/17) | **0.223°** | 0.034° |
| **문서 64 (수리 후, 8/22)** | **0.035°** | 0.031° |

문서 54 §8 이 내건 목표 **"0.223 → 0.05° 이하"** 를 달성했다.
Â 잡음으로 환산하면 0.587° → **0.106°** (문턱 0.6° 의 98 % → **18 %**).
**발목 채널 별도 필터는 넣지 않는다** — 지연 대가를 치를 이유가 없다.

### 6-2. 케이블은 발목 소켓에 고정한다

USB 케이블을 상체에 매달면 발목에서 ~65 cm 의 모멘트암으로 토크가 걸리고,
세션 중 케이블 모양이 변하면서 **동정값이 표류한다** (λ 5.72 → 3.64 로 흘렀던 사례).
**발목 회전축을 지나가게 배선하면 모멘트암이 0 이라 토크가 안 생긴다.**
표류가 사라졌고, 이후 잭나이프 오차가 ±0.073 → ±0.015 로 줄었다.
**측정 조건의 일부로 절차서에 넣을 것.**

### 6-3. 손 접촉 구간은 잘라내야 한다

놓기 → 자유운동 → **손으로 잡음 → 들어올림** 의 순서로 기록되므로,
잡은 뒤 구간을 버려야 한다 (\|φ\| > 8° 이후 폐기). 잔차 상위 표본을 걷어내면
R²(φ̈) 0.876 → 0.983, R²(β̈) 0.571 → 0.929 로 올라간다.
**적합 품질에는 크게 영향을 주지만 r 값은 바꾸지 않는다** (−1.51 → −1.49 수준).

---

## 7. w 식 정정 — ✅ 완료 (2026-08-22)

문서 60 이 쓰던 `w = [−1/r, 1, −1/(rλ), 1/λ]` 는 **감쇠가 0 인 경우의 식**이다.
일반형은 **문서 54 §1 이 이미 적어 두었다:**

> A = w_q·[ q + (λI − D̂)⁻¹ q̇ ]

### 7-1. 유도 — 좌고유벡터 조건에서 바로 나온다

상태 x = (φ, β, φ̇, β̇), 시스템 행렬 A = [[0, I], [K, C]] 에 대해
w 는 불안정 고유값 λ 의 **좌고유벡터**이므로 `wA = λw`. 이를 두 덩어리로 나누면:

```
w_v K = λ w_q                    (앞 두 성분)
w_q + w_v C = λ w_v              (뒤 두 성분)
   →   w_q = w_v(λI − C)
   →   ★ w_v = w_q (λI − C)⁻¹
```

C = 0 이면 w_v = w_q/λ 로 환원되고, 그것이 문서 60 의 식이다.
**문서 54 의 (λI − D̂)⁻¹ 와 같은 형태이며, D̂ = C 다.**

**수치 검증:** 동정된 w_v = [+0.1945, +0.3048] 과
w_q(λI − C)⁻¹ = [+0.1945, +0.3048] 이 **1e-16 까지 일치**한다.

### 7-2. 실측이 요구하는 보정 — 상수 하나면 된다

두 속도 성분의 배수가 조건을 바꿔도 **거의 같다** (비 0.93 ~ 1.00).
따라서 **스칼라 하나**로 고칠 수 있다:

| 조건 | κ_φ̇ | κ_β̇ | 비 |
|---|---:|---:|---:|
| 평활 ±50 / ±70 ms | 1.64 / 1.66 | 1.75 / 1.72 | 0.93 / 0.96 |
| 평활 ±100 / ±130 ms | 2.25 / 2.37 | 2.32 / 2.47 | 0.97 / 0.96 |
| \|φ\| < 2.5 / 3 / 5° | 1.51 / 1.46 / 1.66 | 1.55 / 1.46 / 1.72 | 0.98 / 1.00 / 0.96 |

```
★ w = [ −1/r,  1,  −κ/(r·λ),  κ/λ ]        κ = 1.83 ± 0.35
    = [ +0.664, 1, +0.217, +0.327 ]        (r = −1.506, λ = 5.60)
   무감쇠(κ=1) 이면      [ +0.664, 1, +0.119, +0.179 ]
```

**펌웨어 변경은 런타임 상수 `kv 1.83` 하나 추가로 끝난다.**

### 7-3. 무엇이 바뀌나

**① 속도가 A 의 65 % 다** (문서 54 는 무감쇠 가정으로 50 % 라고 적었다).
불안정 모드 위에서 q̇ = λq 이므로 속도항/위치항 = κ, 즉 속도 비중 = κ/(1+κ) = **65 %**.

**② 문서 50 §0 환율표**

| 놓기 오차 | 문서 50 (모델) | **실측** | 배수 |
|---|---|---|---|
| 몸 기울기 β 1° | +1.00° | +1.00° | 기준 |
| 줄 각 φ 1° | +0.60° | **+0.66°** | 1.1 |
| 몸 각속도 β̇ 1°/s | +0.17° | **+0.33°** | **1.9** |
| 줄 각속도 φ̇ 1°/s | +0.10° | **+0.22°** | **2.2** |

**"각도 1° = 각속도 6°/s" 라는 어림은 실측으로는 1° = 3.1°/s 다.**
놓을 때 잔류 속도 1~2 °/s 가 A 에 넣는 양이 문서 50 의 0.2~0.3° 가 아니라 **0.4~0.7°** 다.

### 7-4. ⚠ 그런데 이것을 "감쇠" 라고 부르면 안 된다

κ > 1 은 `(λI − C)⁻¹ ≈ (κ/λ)I`, 즉 **C ≈ +λ(1 − 1/κ)·I ≈ +2.5·I** 를 뜻한다.
`ẍ = Kx + Cẋ` 에서 **C 가 양수면 속도가 가속을 키운다 — 에너지 유입**이다.
수동 감쇠라면 C 의 고유값이 음수여야 하는데, 동정된 C 의 고유값은 **−0.42 와 +2.45** 다.

**보정 자체는 단단하다** (κ 가 조건을 바꿔도 1.46 ~ 2.43, 항상 1 보다 크다).
**그러나 "감쇠" 라는 해석은 틀렸다.** 후보:

- **측정 안 하는 자유도**가 있고, 그것이 4 상태 사영에서 속도항으로 새어 나온다
- 줄 자체의 동역학 (φ 하나로 줄이 다 표현되지 않는다)

**κ 는 실측 보정 계수로 쓰되, 물리적 감쇠로 해석하지 않는다.**
정체 규명은 §8 미완 목록에 둔다.

---

## 8. 미완 목록

| # | 항목 | 왜 미완인가 | 필요한 것 |
|---|---|---|---|
| **①** | **c₀ (절편)** | 잭나이프 오차 ±0.86 이 값(−1.11)과 같은 자릿수.<br>같은 φ 에서 ank 가 **0.6° 밖에** 안 흩어져 있다 | **보완 측정** — 아래 |
| ② | **κ 의 정체** | 보정값은 확실하나 "감쇠" 로는 설명이 안 된다 (§7-4).<br>C 의 고유값 하나가 양수 = 에너지 유입 | 측정 안 한 자유도 후보 조사 |
| ③ | φ_eq = +1.40° 의 정체 | 엔코더 영점 오차인지 진짜 기울어짐인지 안 갈린다 (§5-1) | 줄 수평 확인 + 반대로 매달아 재현 |

### ★ ① c₀ 보완 측정 — 구체 절차

**문제:** 기울기 r 은 φ 범위가 넓어(−5.6 ~ +8.5°) 잘 잡히지만,
**절편은 그 선에 수직인 분산을 요구**하는데 지금 0.6° 뿐이다.

**해야 할 것:** 같은 φ 에서 **ank 를 자연값에서 ±3° 벗어나게** 두고 놓는다.

| φ | 지금 나오는 ank (≈ 0.33·φ) | **일부러 이렇게도** |
|---|---|---|
| +4° | +1.3 | **−1.7** 과 **+4.3** |
| 0° | 0 | **−3** 과 **+3** |
| −4° | −1.3 | **−4.3** 과 **+1.7** |

- 몸통을 평소보다 **더 세우거나 더 눕힌** 채로 놓는다. 화면의 `ank` 를 보며 맞춘다
- 각 φ 마다 3~4 점, **총 30~40 회**
- 나머지 조건은 부록 A 그대로 (케이블 발목 소켓 고정, 양쪽 방향 섞기)

**얻는 것:** c₀ 가 잡히고, 덤으로 **경계선만으로 r 이 재확인된다**
(놓은 자리와 낙하 방향만 쓰므로 미분·동정·모델이 전혀 안 들어간다).

**A 오프셋 = c₀/r = +0.73° ± 0.6°** — 트리거 문턱 0.6° 와 같은 자릿수다.
**이대로 두면 로봇이 참 평형에서 문턱만큼 치우친 값을 읽는다.**

---

## 9. 조치 목록

| # | 할 일 | 우선도 |
|---|---|---|
| 1 | **펌웨어 `α = ank − phi` 로 수정** (`p2r_test.ino` + 실기 펌웨어 전부) | **최우선** |
| 2 | 문서 52 의 부호 규약 항목에 정정 주석 | 높음 |
| 3 | ~~w 를 감쇠 포함해 재유도~~ → **완료 (§7)**. 런타임 상수 `kv 1.83` 추가 | ✅ |
| 4 | 실기 상수 반영: `p2r 0.4285`, `lam 5.44`, `r -1.506` | 1번 이후 |
| 5 | **c₀ 보완 측정** — 같은 φ 에서 ank ±3° 흩어서 30~40 회 (§8 ①) | 중간 |
| 6 | γ·트리거 문턱을 **새 w (κ=1.83) 로** v21 재스윕 | **가능** |
| 7 | 문서 54 §8 "발목 센서 점검" 완료 표시 | 낮음 |
| 8 | 로그 파일명에 실제 측정일 넣기 (`0817_` 이름의 서로 다른 로그가 셋) | 낮음 |
| 9 | 케이블 = 발목 소켓 고정, 측정 조건으로 절차서 반영 | 낮음 |

---

## 부록 A. 측정 조건 (재현용)

| | |
|---|---|
| 로깅 | 100 Hz CSV (`m` 모드), `t_ms, phi, ank, alpha, del_cmd, del_now` |
| 영점 | 매 세션 시작 시 완전히 멎은 자세에서 `z` |
| **부호 확인** | 문서 50 §1 Z1 — 발/줄을 오른쪽으로 돌려 `phi` +, 몸통만 오른쪽으로 기울여 `ank` + |
| **케이블** | **발목 소켓에 고정해서 빼낸다** (§6-2) |
| 실측 ① | 발목축에서 매달기, δ 0→40→0, 각 점 2 초 정지 |
| 실측 ② ③ | 줄 위, δ 고정, 놓기 30~60 회. **양쪽 방향으로 섞어서** 놓을 것 (§5) |
| 분석 창 | 실측 ②: 놓기 후 ~ \|φ\| > 8° 직전, \|φ\| < 4° 표본만 |

## 부록 B. 재현 스크립트

| 스크립트 | 하는 일 |
|---|---|
| `p2r_fit_0822.py` | 실측 ① — 구간 분할 → 2 초 평균 → α 대 δ 최소제곱 |
| `lambda_fit_0822.py` | 실측 ③ — 시행 분할 → φ_eq 탐색 → ln\|ψ\| 대 t 적합 |
| `r_fit_final.py` | 실측 ② — 자유비행 추출 → 4×4 동정 → 좌고유벡터 → r, c₀ |

★ 세 스크립트 모두 **`α = ank − phi`** 를 쓴다. 펌웨어의 `alpha_deg` 열은 쓰지 않는다.
