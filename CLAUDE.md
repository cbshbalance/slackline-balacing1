# CLAUDE.md — 슬랙라인(한국 줄타기) 균형 로봇 연구 (프로젝트 정의서)

> 이 문서는 **새 대화/새 컴퓨터에서 이 연구를 처음 보는 AI가 일반론으로 새지 않도록** 연구의
> 정체성·기하·물리 시스템·핵심 이론·규약·현재 상태를 정확히 못박는 **권위 문서**다.
> 작업 전에 **이 파일을 먼저 읽고**, 그다음 §8의 권위 문서(.docx 4종)와 §10 작업기록을 참조할 것.
> 최종 갱신: 2026-06-20

> ⚠️ **권위의 위계 (중요)**: 이론의 최신·정본은 **docs의 최근 .docx 4종**(§8)이다.
> docs의 옛 .md(가설설정, FWE_메커니즘_정교화 등)와 v15~17 코드 일부는 **연구의 시행착오 과정**이며,
> 최신 docx와 충돌하면 **무조건 docx가 옳다**. 특히 아래 §3.4의 "분리가정 폐기"를 명심할 것.

> ★ **2026-08-15 추가**: 대회·발표의 성격과 감탄 포인트는 **§0.5** 에 있다.
> 발표 · 영상 · 앱 관련 작업이라면 §0.5 를 §1 보다 **먼저** 읽을 것.

---

## 0. 한 문단 오리엔테이션

**한국 전통 줄타기가 장대 없이 어떻게 균형을 잡는가**를 물리 법칙으로 규명하고 로봇으로 시연하는 연구다.
서양 타이트로프는 **장대의 관성모멘트**로 균형을 잡지만, 한국 줄타기 광대는 **부채만 들고**(부채는
기운 몸을 세울 힘이 없음) 줄을 탄다. 답은 **줄의 처짐(슬랙라인)이 만드는 안정 진자**를 자원으로 쓰는 데 있다.
물리적으로는 **"처진 줄(V자)의 꼭짓점에 발이 고정되어 줄과 함께 흔들리는(원호 궤적) 발 + 허리관절 1개로
구동되는 상·하체 2링크 역진자"** — 3자유도(φ, α, θ)에 입력 1개(힙 토크)인 **저구동(underactuated) 시스템**이다.
두 제어를 다룬다: **(1) LQR**(균형은 잡지만 "어떻게"를 설명 못 함)과 **(2) FWE**(접기-기다리기-펴기,
이 연구의 독자 기여 — 균형의 인과 사슬을 닫힌형 공식으로 드러냄).
산출물: **제72회 전국과학전람회**(물리, 2026-07) 보고서 + 실물 로봇 **'어름이'**(OpenCR + Dynamixel).
참여: 학생 김지훈·송주호·이혜원, **지도교원 정도일**(이 레포 주인).
선행연구: Paoletti & Mahadevan(2012)이 같은 역학모델로 "제어 가능"까지만 보였고, "어떤 물리과정으로
복원되는가"는 미규명 — 그 빈자리가 이 연구의 표적이다.

---

## 0.5 이 연구는 무엇을 위한 것인가 — 대회·발표의 성격 ★ (2026-08-15 추가)

> **새 AI가 가장 크게 빗나가는 지점이다.** 여기를 읽지 않으면 논문 대회 기준으로 정밀도와
> 완결성을 추구하다가, 정작 이 연구가 이겨야 하는 판을 놓친다.

### 가. 전국과학전람회는 논문 대회가 아니다

- **발표 점수가 보고서 점수보다 압도적으로 크다.**
- 보고서는 **중간보고서**다. 제출 후 한 달 동안 거의 다른 탐구로 볼 수 있을 만큼 탐구를 더
  진행해 발전시킨다. **보고서의 오류나 낡은 내용은 발표에서 바로잡으면 된다.**
  → 앱·시뮬의 숫자가 보고서와 달라도 된다. 오히려 **최신값이어야** 한다.
- **3분 발표 + 10분 질의응답.** 질의응답이 3배 길다. 통합 앱의 진짜 무대는 질의응답이다.
- 3분에 세세한 검증은 불가능하다. 검증은 **질의응답용 재료**로 준비해 둔다.
- 목표: 심사위원이 **빠르게 문제 상황을 이해 → 공감 → 결과와 과정에 감탄**하게 만드는 것.

### 나. 연구의 정체 — 메커니즘 제안이지 인간 관찰 연구가 아니다

- 우리는 **줄타기의 메커니즘을 제안**했고 그것을 **로봇으로 구현**했다.
  **로봇 제어공학에 물리적 영감을 제공하는 연구**로 자리매김한다.
- **실제 광대가 우리 대로 한다는 보장도, 일치할 필요도 없다.** 광대의 회복 각속도 측정 같은
  것은 문서 30의 서술 경계선대로 *검증 가능한 예측 / 후속 과제*로만 둔다.
  절대 우리 주장의 근거로 쓰지 않는다.
- 검증 축은 셋뿐이다: **닫힌형 이론 ↔ MuJoCo 전물리(±2%) ↔ 실기 '어름이'.**
  **실사 영상은 검증 자료가 아니다.**

### 다. ★클라이막스 — 최대 감탄 포인트

> **복잡하고 불안정한 한국 줄타기 시스템에서, 균형에 필요한 조건을 해석적 닫힌해로 얻어냈다.**

- 닫힌해가 있으려면 식이 있어야 하고, 식이 있으려면 모델과 메커니즘이 있어야 한다.
  **지금까지 그 누구도 한국 줄타기를 이런 식으로 해석한 적이 없다.**
- 가장 가까운 선행연구 Paoletti & Mahadevan (2012, *J. R. Soc. Interface*) 조차
  ① **장대를 쓰고** ② 균형이 **가능하다는 사실만** 시뮬레이션으로 보였을 뿐
  ③ **무너진 균형을 어떤 물리 원리로 회복하는지는 전혀 설명이 없다.**
- **장대 없이 흔들리는 줄 위에서 균형을 잡는 원리를 물리적으로 해석해 낸 연구는 우리가 세계 최초다.**
  발표는 이 지점을 **클라이막스**로 삼고 여기서 심사위원의 감동을 끌어낸다.

### 라. 마지막 방점

해석해에서 끝나지 않았다. **실제 로봇에 적용할 수 있는 제어 메커니즘**을 찾다가
**증분 접기**를 발견하고 완성했으며, 그것을 적용한 로봇으로 **실제 균형잡기에 성공**했다.
**클라이막스(닫힌해) → 방점(실증)** 의 순서를 지킨다. 방점이 클라이막스를 가리지 않게 한다.

### 마. 보조 감탄 포인트 (클라이막스를 옆에서 받친다)

- **모드 분석**이라는 대학 수준의 수학·역학을 고등학생이 **먼저 정성적으로 추론**한 뒤
  AI의 도움을 받아 이해하고 적용했다는 점.
- **상태공간 예측점 평면**이라는 도구를 만들어 이 모든 과정을 **정성적으로 보이게** 만든 점.

### 바. 발표 서사 곡선 (씬 순서 고정 — 임의 재구성 금지)

| 씬 | 내용 | 역할 |
|---|---|---|
| ① | 실사 줄타기 → 정지 → 같은 구도로 v19 오버랩 | 역설 제시 |
| ② | 줄 간격이 실기와 같은 약 600 mm 로 축소 | "그네가 아니라 필요한 부분만" |
| ③ | xz 평면 등장 → 전체 화면으로 펼쳐지며 2링크 모델 | 무대를 평면으로 |
| ④ | (φ, α, θ) 제시 → (φ, β, δ) 로 갈아타기 | 좌표가 갈라진다 |
| ⑤ | 3D 복귀, 메커니즘: 접고 · 기다리고 · 펴고 | 인과 사슬 |
| ⑥ | 안정모드선 포함 (β, φ) 상태공간 → 예측점 평면 추가 | 도구 |
| ⑦ | **ε\* — 닫힌해** → 증분 접기 결과 | **★클라이막스** |
| ⑧ | 실기 '어름이' 주행 | 방점 |

### 사. 실사 영상의 역할 — 정확히 셋, 그 이상 아니다

1. **도입부 역설** — 장대도 없이 처진 줄 위에 선다.
2. **후크** — 4초 지점의 휘청 자세. 상체가 왼쪽으로 접혀 있고(θ<0 → δ<0) 엉덩이가 반대쪽으로
   빠져 **발을 왼쪽으로 보내는 접기 자세**다. ④에서 이 자세에 **δ라는 이름이 붙고**
   ⑤에서 시뮬이 같은 자세를 만든다 — 도입의 사진을 끝에서 회수한다.
3. **모델 인지** — 우리 모델이 그네처럼 생겨서 어떻게 줄타기를 표현하는지를 **빠르게**
   이해시켜야 이후 설명이 이어진다.
   ⚠ **보통 사람은 줄 위에 서서 균형을 잡는 상황이 아니라 '줄 위를 걸어가는' 상황을 상상한다.**
   이 오해를 먼저 걷어내야 한다. 보고서 **그림 4(표지 그림)** 가 그 답이다 — 실사 위에
   xz 평면 · z/y/x 축 · R · φ 를 그려 *"우리 줄타기 모델은 x-z 평면상의 흔들림을 표현하는 것"*
   임을 한 장으로 세운다. 오버랩이란 **로봇을 겹치는 것이 아니라 이 그림을 살아 움직이게 하는 것**이다.

### 아. 새 AI가 실제로 저지른 실수 (반복 금지)

- 실사 영상을 **검증 자료**로 취급했다 → 영상 소스 찾기 · 학생 직접 촬영 제안으로 샜다.
- **사람이 실제로 우리 메커니즘대로 하는지** 확인하러 가려 했다 → 연구 목적이 아니다.
- 씬 순서를 **임의로 재구성**했다 (§0.5-바 표로 고정이다).
- 논문 대회 기준의 정밀도 · 완결성을 추구했다 → 이 대회는 3분 + 10분이다.
- 없는 것을 만들기 전에 **있는 것부터 확인**하지 않았다 (표지 그림이 이미 답이었다).

---

## 1. ⚠️ 용어·오해 방지 (제일 먼저 읽을 것)

새 AI가 매번 틀리는 지점들. 여기서 어긋나면 연구 전체가 일반론으로 샌다.

- **슬랙라인(slackline) 연구다. 타이트로프(tightrope)가 아니다.** 줄이 **처져서**, 그 위에 선 사람 때문에
  줄이 **V자**(두 발이면 사다리꼴이나 V로 간주)가 된다. V 꼭짓점의 **처짐 길이**는 기둥간 거리와 줄 길이로
  **피타고라스**식으로 정해지며, 이 처짐 길이를 **반지름 R**로 하여 **줄과 발이 함께 좌우로 흔들린다**. 이때
  발이 그리는 궤적이 반경 R의 원호이고, 중력 복원력이 생기는 **안정 진자(정진자)** 가 된다. 팽팽한 줄 +
  장대 관성의 서양식과 물리가 근본적으로 다르다. (프로젝트 제목에 "tightrope"라 쓰지 말 것.)
- **★발은 줄 위를 미끄러지지 않는다 (매번 AI가 틀리는 지점).** 발은 V 꼭짓점에 **고정**되어 줄과 한 몸으로
  흔들린다. "정적인 줄(원호 레일) 위를 발이 구슬처럼 미끄러진다"는 그림은 **틀렸다.** 발의 *궤적*이 원호라
  동역학적으로 "원호 레일 위 카트"로 **등가 추상화**할 뿐, 물리적으로 줄을 따라 미끄러지는 것은 없다.
- **줄(φ)은 "안정" 요소다.** 흔히 줄을 불안정 요인으로 오해하나 반대다. 줄 진자는 중앙 복원하는 **안정 모드**,
  **불안정한 것은 가중 무게중심 기울기 β**다. 제어가 필요한 건 β(의 불안정 모드)다.
- **장대(balance pole) 안 씀.** 균형은 오직 **허리 접기 1관절**로. 장대/플라이휠/부채흔들기 류 제안 금지.
- **줄과 몸은 분리되지 않는다(★).** "줄 진자와 몸 쓰러짐이 독립"이라는 **분리가정은 틀렸고 폐기됐다**
  (MuJoCo 대비 3~4배 오차). 발의 가속도가 몸 쓰러짐에 관성력을 만드는 **질량행렬 커플링이 본질**이다(§3.4).
- **액추에이터 1개**, 3자유도. "1개로 왜 3자유도가 잡히나"가 출발 질문 — 답은 관성 커플링으로 단일입력이
  φ까지 간접 제어/관측한다는 것(가제어·가관측 rank 6 확인).

---

## 1.5 선행연구(Paoletti) 모델과 우리 모델 — "단일 → 이중 → 단일"의 계보 ★

새 AI가 우리 기여를 일반론으로 뭉개지 않도록, 선행연구와의 차이를 못박는다.

- **Paoletti & Mahadevan (2012, J. R. Soc. Interface, "Balancing on tightropes and slacklines")**:
  줄 위 균형을 **"원호 트랙 위 카트(각 φ) + 그 위 단일 강체 역진자(길이 l·꼭대기 집중질량 m, 각 α)"** 로
  모델링하고 **최적제어(LQ/LQG)** 로 안정화. 핵심 결과: (i) 제어기로 균형이 **가능**하다, (ii) **노력이 최소가
  되는 최적 줄 처짐 R(sag)이 존재**한다. 그러나 몸을 **단일 강체**로 보고 **추상적 복원 토크**(발목/베이스 토크
  격)를 가정 — "장대 없는 광대가 그 토크를 실제로 어떻게 만드나", "어떤 물리 과정으로 복원되나"는 미규명.
  (우리 φ·α 표기 계보가 여기서 옴. R도 그들의 sag 매개변수와 같은 의미.)
- **우리의 출발 = 단일 → 이중**: 한국 줄타기는 **장대도 없고 발목 토크 권위도 사실상 없다**(줄=점접촉).
  그래서 Paoletti의 *추상 토크·단일 강체*를 버리고, **실제로 존재하는 단 하나의 입력 = 허리(힙) 관절**을
  쓰려면 몸을 **상·하체 2링크(이중 역진자)** 로 모델링해야 한다. → 우리는 단일을 **이중 역진자로 확장 제안**.
- **다시 단일로 회귀(공교롭게)**: (φ,β,δ) 모드해석에서 토크는 δ에만 작용하고, 균형은 **단일 불안정 모드 β**로
  환원된다(§3·§5). 즉 "정진자 위 단일 역진자" 문제로 사실상 돌아온다 — **단, 구동 경로가 근본적으로 다르다**
  (발목/베이스 토크가 아니라 **힙 토크로 간접 구동**, 진짜 저구동).
- **차이 요약**: ① 몸 모델 — 단일강체 vs **2링크**, ② 입력 — 추상 복원토크(≈발목) vs **힙 토크 1개**(저구동·장대無),
  ③ 기여 — Paoletti는 "제어 가능 + 최적 sag 존재"(*what*), 우리는 **FWE 인과 메커니즘 + 닫힌형 접기비율**(*how*),
  ④ 공통점 — **최적 R(sag)의 존재**(Paoletti 최적 sag ↔ 우리 적정 R 0.6~0.8m, 사람스케일)가 서로를 보강.
- **작업 제목**: "정진자 위의 **이중** 역진자"로 간다(최종 제목은 추후 확정). 보고서 일부에 "단일 역진자"로 적힌 건
  실수지만 사실상 단일 문제로 환원되는 게 공교로운 지점 — 그래도 Paoletti 단일 모델과는 위처럼 다르다.

---

## 2. 핵심 기하·좌표 시스템

```
기둥(고정점) ─[φ] 줄(원호 반경 R, 정진자·안정)
                 └─[α] 하체(발→힙, 역진자) ─[δ=힙토크 τ]─ [θ] 상체(힙→머리, 역진자)
```

### 2.0 줄 처짐과 R (피타고라스 — 개념 출발점)
기둥 사이 거리 D, 줄 길이 L(>D), 줄꾼이 중앙(midspan)에 설 때 발은 V 꼭짓점에 있다.
무질량 줄(직선 두 토막) 가정에서 **처짐 길이 = R**:
> **R = √((L/2)² − (D/2)²) = ½·√(L² − D²)**

(반쪽 줄 L/2가 빗변, 수평 절반 D/2가 밑변, 처짐이 높이 → 피타고라스. 발은 이 V 꼭짓점에 고정되어
줄과 함께 반경 R로 흔들린다 — 미끄러지지 않음.)
⚠️ **단, 실물에서는 R을 직접 측정**(베어링축~발 거리, §6.2)하므로 이 식은 개념적 출발일 뿐 실제 계산엔 안 씀.

### 2.1 일반좌표 (φ, α, θ) — 모두 연직 기준, 오른쪽 기울기 +
| 기호 | 정의 |
|---|---|
| **φ** | 줄+발이 함께 흔들리는 각도(발이 그리는 원호 궤적). 발 위치 `x_foot = R·φ`, R=처짐 길이(피타고라스). 발은 줄을 따라 미끄러지지 않음 |
| **α** | 하체 절대 기울기 |
| **θ** | 상체 절대 기울기 |
| **δ = θ − α** | 힙 관절각(접힘각). **δ>0 = 앞으로 접기**: 머리와 발이 모두 오른쪽, 엉덩이(힙)가 왼쪽으로 → 몸이 접힘 (θ>0·α<0). **δ<0 = 뒤로 접기**: 머리·발 왼쪽, 엉덩이 오른쪽 |
| **τ** | 힙 토크. 입력 `E=[0,−1,+1]` (α에 −τ, θ에 +τ). LQR: `τ=−K·x` |

> ⚠️ **δ 해석 함정 (매번 AI가 틀리는 지점).** δ>0을 "상체가 하체보다 오른쪽"이라고 적으면 *틀린다*. 식으로는
> θ>α가 맞지만, 물리 그림은 **머리·발이 같은 쪽(오른쪽)으로, 엉덩이는 반대쪽(왼쪽)으로 빠지며 몸이 V로 접히는 것**
> (사람이 오른쪽을 보고 허리를 앞으로 숙인 자세). "상체가 한쪽으로 평행이동"하는 그림이 아니라 **힙을 꼭짓점으로 접힌다.**

- LQR 상태벡터 `x = [φ, α, θ, φ̇, α̇, θ̇]` (6D). φ는 직접측정 불가 → 칼만 추정.

### 2.2 모드 좌표 (φ, β, δ) — FWE/모드해석의 본진 ★
| 기호 | 정의 | 의미 |
|---|---|---|
| **β** | **(p₁α + p₂θ)/(M_t·h)** = 가중 무게중심 기울기 | `x_CoM = R·φ + h·β`. β가 진짜 균형(쓰러짐) 변수 |
| **δ** | 접힘각 θ−α | 제어입력이 직접 구동하는 자세변수 |

- 역변환(놀랍도록 단순, `M_t h = p_s` 사용): **α = β − (p₂/p_s)δ,  θ = β + (p₁/p_s)δ.**
- **이 좌표의 핵심 구조 3가지**(유한시간접기 노트에서 기호검증 완료):
  1. **토크는 δ에만 작용**한다 (φ, β 동역학에 τ 세부가 안 들어옴 — 서보가 δ 추종하는 한).
  2. **접기 각가속도 δ̈가 (φ,β)로 들어오는 문은 β행의 관성항 μ 단 하나**다.
  3. **접힌 자세의 중력효과는 φ,β에 전혀 안 들어온다** (β가 중력 가중평균으로 정의됐기 때문).

### 2.3 ⚠️ 부호 함정
- **MuJoCo는 `x_foot = −R·φ`**, 해석모델은 `+R·φ`. **비교 시 φ 부호 변환 필수**(안 하면 가속도 ~10배·역방향).
- 엔코더↔δ 부호: 펌웨어 간 불일치(step4b `δ=−encoder` vs lqr_balance "반전 제거") — **실물 검증 필요**(§7).

---

## 3. 물리 시스템 — 결합 모드 해석 (★이 연구의 물리적 심장)

### 3.1 운동방정식 (소각 선형화)
`M·q̈ = G·q (+ E·τ)`,  q=(φ,α,θ)ᵀ
```
M = [[Mt·R²,  R·p1,  R·p2],     G = diag( −Mt·g·R,  p1·g,  p2·g )
     [R·p1,  J_αα,  J_αθ],      # φ²항>0=안정(복원), α²·θ²항<0=불안정(발산)
     [R·p2,  J_αθ,  J_θθ]]
```
`J_αα=m1·LC1²+I1+m2·L1², J_θθ=m2·LC2²+I2, J_αθ=p3=m2·L1·LC2`.
정적모멘트 `p1=m1·LC1+m2·L1, p2=m2·LC2, p_s=p1+p2, h=p_s/Mt`.
- **G는 대각, M은 비대각(R·p1, R·p2, J_αθ).** 이 비대각 관성항이 모든 흥미로운 거동의 근원.

### 3.2 무게중심 보존법칙 (가장 깨끗한 구조)
> **ẍ_CoM = −g·φ(t)** — δ(t)·τ가 무엇이든, **접는 도중에도 정확히 성립**(기호검증 완료).

무게중심은 "발 위치 Rφ에 비례한 옆밀침을 받는 자유입자"다(무질량 줄은 줄 방향 힘만 줌).
순간동작 극한에서 x_CoM·ẋ_CoM 불변인 이유이자, **상태점프가 `RΔφ + hΔβ = 0`(무게중심 등고선,
기울기 −h/R)을 따라 미끄러진다**는 보존법칙의 근거.

### 3.3 (φ,β) δ-고정 2×2 부분계와 모드 분해
Wait 중 δ 고정 → (φ,β) 2자유도: `M₂₂·q̈ = G₂₂·q`, `A₂ = M₂₂⁻¹G₂₂`.
A₂를 고유분해하면 **부호 반대인 두 실모드**:
- **안정 모드** λ₁<0: `cos/sin(ω_eff t)` — 줄–무게중심 **반진동**(φ↔β 반대부호, 줄꾼이 엇갈리게 타는 그것).
- **불안정 모드** λ₂>0: `cosh/sinh(λ_eff t)` — 발산. **배가시간 0.2~0.3s** — 모든 전략은 이 안에서 작동해야.

핵심 수치(사람스케일, §6 파라미터):
| R [m] | ω_eff | λ_eff | 안정주기 2π/ω_eff [s] | r=v₁φ/v₁β (안정모드비) | ε* |
|---|---|---|---|---|---|
| 0.3 | 10.97 | 3.10 | 0.57 | −4.39 | 2.68 |
| 0.5 | 7.95 | 3.32 | 0.79 | −2.78 | 2.22 |
| 0.7 | 6.30 | 3.54 | 1.00 | −2.12 | 1.83 |
| 1.0 | 4.83 | 3.86 | 1.30 | −1.66 | 1.38 |
| 2.0 | 2.84 | 4.65 | 2.21 | −1.23 | 0.64 |

### 3.4 ⚠️ 폐기된 시행착오: "분리가정"과 옛 메인식
- **분리가정**(φ̈=−ω²φ, β̈=λ²β 독립, `ω=√(g/(R−h))` 단일진자)은 **MuJoCo 대비 복원시간 3~4배 과대예측**
  → **폐기.** 원인은 관성 커플링 무시(c_φ/ω²_diag ≈ 81%로 대각항과 같은 자릿수).
- 옛 메인식 **`(1+ε)cos(ωT)=ε·cosh(λT)`**, ω=√(g/(R+h)), λ=√(Mgh/I) 는 **이 분리 단순모델의 산물**이라
  정량적으론 폐기. (단 "β₀ 소거 → 최적 T가 진폭 무관 → 비례제어가 되는 이유"라는 **정성적 통찰은 유효**,
  결합모델의 ε*가 β₀ 무관인 것으로 계승됨.) **새 AI가 이 식을 핵심 결과로 인용하면 안 됨.**

---

## 4. 제어 전략 ① — LQR (선형 안정화, 로봇 '어름이' 1차 목표)

- 연속 선형모델 → **ZOH 이산화(DT=0.002, 500Hz)** → **DARE**로 K. `Q=diag([1,50,50,0.1,5,5])`, `R=0.001`.
- **칼만 관측기**: 측정 {α,θ,α̇,θ̇}(또는 엔코더 δ + IMU) → 6상태(φ 포함) 추정. φ 보조추정 `φ≈−(C1·α+C2·θ)/R`.
- 모드좌표에서 본 LQR 구조: **접힘게인 K_δ는 R 무관 상수**(≈−81.5), 두 모드에 거의 같은 감쇠비 →
  "LQR은 두 모드를 같은 방식으로 동시에 다룬다". FWE와 같은 기하(상태를 안정모드로 사영)의 다른 표현.
- 계산: **`opencr_test/compute_K_measured.py`**(실측·전류제어 대응, 권장). 구버전 `compute_K.py`(DT=0.001, 균일막대).

---

## 5. 제어 전략 ② — FWE (접기-기다리기-펴기) ★연구의 독자 기여

**모드해석에 기반한 최적 접기 비율의 닫힌형 유도.** LQR이 숨긴 균형의 인과 사슬을 명시한다:
*접어서 발을 무게중심 너머로 보내 → 중력 토크 방향 반전 → 상태를 안정모드에 정렬한 채 기다린다.*

### 5.1 순간접기 상태전이 맵 (가정: 소각·무질량줄·순간접기·Wait중 δ고정)
접기 비율 ε(과접기 비율)로 매개화:
> **β_new = −ε·β_i,   φ_new = φ_i + h(1+ε)β_i/R** (x_CoM 보존), 속도는 보존.

발 이동량 `Δx_foot = h(1+ε)β_i`. ε=0이면 발이 CoM 바로 아래, ε>0이면 CoM을 **지나쳐** 감.

### 5.2 닫힌형 결과 4종 (난이도 순)
1. **정지출발 ε\* = −h/(rR+h)** — 접기 직후 상태를 안정모드 고유벡터에 정렬(c₂=0)시키는 조건.
   r은 안정모드 고유벡터비(§3.3 표). **β₀(초기기울기)에 무관** → 비례제어 가능의 물리적 근거.
2. **비정지 일반화 ε\*_gen = −u₂ᵀ·q_pred,base /(β_i·u₂ᵀ·e)**,  e=(h/R,−1)ᵀ,
   q_pred,base=(φ_i+hβ_i/R+φ̇_i/λ_eff,  β̇_i/λ_eff)ᵀ. **안정조건은 c₂=0이 아니라 A=(c₂+d₂)/2=0**
   (속도를 불안정모드 시정수로 환산한 *예측상태*를 안정모드에 정렬). 정지대입 시 ε*로 정확 환원(10⁻¹⁵),
   이미 균형이면 ε=−1(무개입)이 나옴(자기일관성).
3. **유한시간 접기 ε\*_ft = (1+ε\*_gen)·γ(x)**,  x=λ_eff·T_f,  γ=e^x/B(x).
   삼각프로파일 `γ_tri≈1+x/2+5x²/48`. **γ>1 항상** → 유한시간 접기는 순간보다 **항상 더 접어야**
   (사람스케일 T_f≈0.25s에서 **+53%**). T_f→0이면 γ→1로 순간접기 정리 복원. → **빠른 서보 필수.**
   요구 관절각 `δ_f* = (1+ε*_ft)·β_i·(J_β−Mt·h²)/μ`. **μ 불확실성은 δ_f*에만, ε*·γ엔 약분돼 안 들어감.**
4. **완전 사이클 ε_FWE**(과접기-대기-펴기): `1+ε_FWE = (1+ε*_gen)/(1−e^{−λ_eff·T_w})`,
   유한시간 포함 `=(1+ε*_gen)·γ(λT_f)/(1−PV_e/PV_f·e^{−λT_w})`. **T_w = 안정주기의 정수배(n·T_osc)**
   가 사이클 후 진동진폭 최소(현재가치 장부 해석). **'정확소거 후 펴기'는 실패**(펴기 고정킥이 발산성분
   되살림) → ε*보다 **과접기**해서 반대부호 잔여를 만들고 그게 e^{λT_w}로 자라 펴기킥과 같아질 때 편다.

### 5.3 "얼마나 접나 / 기다리나 / 펴나" 완전한 답
- **얼마나 접나**: `(1+ε*_gen)·γ(λT_f)/(1−e^{−λT_w})`.
- **얼마나 기다리나**: ε*로만 접으면 **발산이 없어 상한 없음**(한 번 접으면 펴지 않아도 유계 진동 유지!).
  펴기를 포함하면 **안정주기의 정수배**가 자연 최적(관절한계가 하한 결정).
- **어떻게 펴나**: 빠르게(역프로파일 재생이면 충분).
- 광대의 언어: *"기운 만큼보다 조금 더 접어 반대로 살짝 넘어지게 만들고, 줄이 한 번 갔다 돌아오는 박자에 편다."*

### 5.4 ⚠️ 폐기된 시행착오 (옛 .md와 충돌 시 무시)
- 옛 `FWE_메커니즘_정교화_20260518.md`의 **"4단계 F-W₁-E-W₂ + 자기일관성(총시간=φ=0 복귀시간)"** 은
  **과도기 프레이밍**. 정답은 위 §5.2~5.3(과접기-대기-펴기, T_w=n·T_osc). 자기일관성 timing은 폐기.

### 5.5 검증 현황 (MuJoCo 전물리)
- **사람스케일**: T_cross 해석/MuJoCo **±1%**, ε*·ε*_gen 수치최적과 **±2%(R≥0.6~0.7)**. ε*에서 한 번 접기로
  발산 없이 진동균형 진입 확인. 비정지서 정지공식 쓰면 1.1~1.6s에 실제 낙하(β>60°) — 일반화 필요성 실증.
- **소형로봇**: ε* 경향성 확인, PD 서보게인 튜닝(ωn=200, ζ=0.7 → δ유지 0.25°). δ_fold 편차 10%+라 경향만.
- R=0.3 같은 작은 R은 φ_max가 커져(23.5°) 소각근사 깨짐 → 오차↑. **적정 R 0.6~0.8m(사람스케일).**

---

## 6. 하드웨어 / 실물 로봇 '어름이' (★3개 스케일 혼동 금지)

세 스케일이 공존한다. **절대 섞지 말 것.**

1. **이론·MuJoCo 검증 스케일 = 사람**: M1=30, m2=45kg, Mt=75, L1=0.85, LC1=0.30, LC2_eff≈0.40~0.467m,
   p1=51, p2=21, h=0.96m, R 0.3~2.0. **J_β=89.03, μ=4.33, J_δ=2.03 kg·m².** (★p1·p2는 *유효값*,
   명목길이 직접대입 47.25·18.00과 다름 — 검증수치는 모두 유효값 기준.)
2. **형상최적화 목표(6/15)**: H=80cm(되면 100), **하체:상체=30:70(24:56cm)**, 총~3kg, **장비를 머리/상체 위쪽**
   (I_cm↑+접기 앵커 win-win), **발끝 비움**(접점=구동카트, 질량금지), 목표 h≈43cm, R=0.4~0.5m.
   레버: **전체 길이↑가 큰 레버**(λ_eff≈L^−½), 분배는 부차. 저중심 무게추 금지(발던지기 4~5배 붕괴).
3. **현재 제작 프로토타입 = v11_small급**: m1=0.10, m2=0.30kg, L1=0.20, L2=0.25m, R=0.30(총~0.4kg, ~45cm).
   `compute_K.py`·`lqr_balance.ino`가 쓰는 값. **직립유지는 되나 큰 각도 복원은 경계선** → 바로 FWE 가치 구간.

### 6.1 전장
- **MCU** ROBOTIS **OpenCR 1.0**. **IMU** 내장 **ICM-20648**(보드에 있음 → **상체 상단, 힙축에서 ~13cm 위**).
- **모터** Dynamixel **XM430-W210-R**(RS-485, ID1, Proto2.0). 힙 1관절.
  스톨 **3.0 N·m@12V·2.3A → Kt≈1.304 N·m/A**(공칭). Goal/Present **Current 단위 2.69 mA/unit**,
  **Current Limit 1193 unit(=3.21A)**. **제어주기 500Hz(DT=0.002)**. 전원 12V.

### 6.2 줄의 실물 구현 (모델-실물 갭을 의도적으로 좁힘)
- 3mm 강선을 **베어링으로 매달아 거의 강체 + X축(시상면)만** → 모델의 R-원호·평면 구속을 충실 구현.
- 효과: **좌우/yaw 넘어짐 실패 클래스 제거**, **R=베어링축~발 거리로 정확히 측정됨**(R 불확실성 제거).
- 추가 반영: **펜듈럼 팔 자체 관성** → M_eq[0][0]에 더하기(로봇 없이 진자만 흔들어 측정),
  **베어링 마찰 = φ 감쇠**(대체로 도움, 단 **스틱션 데드밴드** 주의).

---

## 7. 현재 상태 & 미해결 이슈 (2026-06-20)

1. **펌웨어가 전류제어 아님(★토크 보정 핵심).** `lqr_balance.ino`는 `OP_EXTENDED_POSITION`(위치제어)에
   `δ_goal=δ−τ/KP_EQUIV`(=8.0)로 토크 흉내 → LQR 설계토크 ≠ 실제토크. **신규 `lqr_balance_current/`가
   `OP_CURRENT` 직접명령**(`GoalCurrent_unit=τ×285.08`, TAU_MAX_UNIT=855). `prev_tau`=전류cap 반영
   실제적용 τ. **MOTOR_DIR(±1)·엔코더↔δ 부호는 브링업에서 결정**(절차 `docs/전류제어_단일칼만_설계노트_20260622.md` §5). 보정 스케치 `torque_cal/`. → **실물검증 대기.**
2. **θ 추정 폭주 — 단일 모델기반 칼만으로 해결(2026-06-22).** 원인 확정: (a) 위치제어의 *가짜 토크*로 칼만
   예측 `Bd·τ`가 실제와 어긋나 자기반작용을 외란처리 → 양의 피드백, (b) CF가 만든 θ를 칼만 측정으로 재투입(직렬
   중첩) + `α=θ_cf−δ`라 θ오차가 α로 오염. **해결**: 전류제어로 진짜 τ 사용 + CF 제거, 물리측정 직접 투입 —
   z=[θ_acc(1g게이팅), θ̇_gyro, δ(엔코더), δ̇(PRESENT_VELOCITY)], C=[e3; e6; e3−e2; e6−e5]. α·φ는 측정
   안 하고 모델 커플링+엔코더 구속으로 복원(CF가 칼만에 흡수됨). 관측 rank6·관측기 안정(0.9937), 사이드 시뮬상
   추정오차 0.0004°·θ폭주 없음. (오래 큰 구동엔 EKF 선형가속 보상으로 격상 여지.)
3. **α̇ 노이즈 해소**: `lqr_balance_current`는 엔코더 생차분 대신 `PRESENT_VELOCITY` 직접 읽어 δ̇로 사용.
4. **실측 미완(★현 임계경로)**: m, ℓ(LC), I, Kt 안 잼. 펌웨어 상수는 v11_small *예시값* — 실측 후 전부 교체.
   절차서 `docs/실측_캘리브레이션_절차_20260620.md`(복합진자법 등).
5. **수정완료**: DT 불일치(구 0.001→0.002; `compute_K_measured`/신펌웨어 Ad는 0.002 일관), TAU_MAX 4.0→3.0.
6. **주의**: LQR 극이 느려(≈1 rad/s) 작은 로봇서 과도피크 큼(IC 0.5°→3.8°, 2.9°→22°, 무노이즈) — §6.3
   "큰 각도 복원 경계선"과 일치, FWE 표적 구간. 57600 baud 500Hz에서 pos+vel 읽기 대역 빠듯하면 1Mbps로 올릴 것.

### ★2026-08-07 추가 — 미처리 조치 2건 (다음 세션이 먼저 볼 것)

7. **`fwe_async_v19.ino`의 `PROF_ACC_UNIT = 250`이 모터 능력의 1/5 — 600으로 올릴 것.**
   토크·속도 수지 재계산 결과(프로젝트 문서 40): **토크는 남고 속도가 병목**이다.
   ★혼동 금지 — 서보가 느끼는 유효관성이 두 개다: **줄 위(자유) 0.0059 kg·m²**
   (모달 J_δ 0.0076의 Schur 보수 0.0029 + armature 0.0030) vs **지그·손으로 잡음 0.0477**
   (M2 실험의 I_δ=0.045가 이쪽). 8배 차이. 줄 위 기준 acc250이 요구하는 토크는 0.55 N·m
   = 스톨 3.0의 18%뿐이고, 접기는 1~2ms 만에 속도한계(무부하 462°/s)에 붙는다.
   → 배가시간 104ms(λ_u=6.68 실측) 안에 접을 수 있는 최대가 32° = **회복 천장 β≈4.0°**
   (시뮬 A*≈3°와 정합). acc 600이면 20° 접기 122→81ms, 천장 β 2.8→3.6°.
   ⚠벤치에서 잡을 사람 두고 적용 — 결합부 7 Hz(문서 32·33 미해결)를 더 세게 두드린다.
8. **`v19_bringup/params_v19.py`가 placeholder로 되돌아가 있음**(8/6 저녁 push로 실측값·
   c_phi 정정 소실). compute_gains/sim_check가 틀린 값을 쓴다. 실측 정본:
   m1=0.1094, m2=0.6406, L1=0.259, L2=0.375, ell1=0.084, ell2=0.1917,
   I1_cm=8.85e-4, I2_cm=2.116e-2, I_r=4.50e-3, c_phi=3.3e-4, tau_coulomb=3.1e-4.

### 다음 단계
실측 → `compute_K_measured.py`로 K·L 재계산 → `lqr_balance_current` 상수 교체 → 브링업(MOTOR_DIR·부호) →
gain_scale 점진 검증 → FWE 이식.

---

## 8. 레포 구조 / 핵심 파일 지도

### ★ 권위 문서 (이론 정본 — docs의 최근 .docx 4종)
- `한국 줄타기 로봇의 FWE 균형 제어 메커니즘(커플링 물리 적용 완결본).docx` (6/10) — **메인 논문**.
  결합모드해석, ε*·ε*_gen 닫힌형, 분리가정 폐기, MuJoCo 검증 전체.
- `유한시간접기_유도노트.docx` (6/10) — (φ,β,δ) 분리구조, ẍ_CoM=−gφ, **γ 보정**(ε*_ft).
- `펴기효과와_FWE완전사이클_v2.docx` (6/12) — 무게중심 등고선, **과접기-대기-펴기 ε_FWE**, T_w=n·T_osc.
- `유한시간_펴기와_상태공간궤적_유도노트.docx` (6/21) — **마스터 전파법칙**(δ경계 일반화), 접기/펴기 비대칭(킥 반대·시간이자 동방향), 유한시간 완전사이클 δ_f* 닫힌형(수치 정확검증), (φ,β) 궤적 그림. 스크립트 `analysis/fwe_finite_state_space.py`.
- `한국 줄타기 로봇의 FWE …(전국과학전람회 형식 보고서 초안).docx` (6/10) — 전람회 보고서(서사·목차).

### 시행착오/과정 기록 (참고만, 충돌 시 docx 우선)
- `슬랙라인_균형제어_가설설정_*.md`(옛 메인식 분리모델), `FWE_메커니즘_정교화_20260518.md`(4단계 자기일관성),
  `모드분석_LQR게인구조_20260607.md`(초기 모드분석, ω_eff·λ_eff 수치 수% 차이 — docx가 정본).
- `FWE_WaitPhase_모드분석_eps_star_20260609.md`, `FWE_일반화_eps_star_gen_20260609.md` (ε* .md판; docx에 통합됨).
- `형상최적화_상하체비율_20260615.md` (30:70, h≈43cm — 형상 결론은 유효), `실측_캘리브레이션_절차_20260620.md`.

### 코드
- **`opencr_test/`** — 실물 펌웨어·게인. `compute_K_measured.py`(권장)/`compute_K.py`(구),
  `lqr_balance_current/`(★전류제어+단일 모델기반 칼만, 신규·실물검증대기), `lqr_balance/`(위치제어 LQR+칼만, 폴백), `torque_cal/`(전류제어 토크보정),
  `step1~5/`·`motor_hold_180/`(단계 테스트), `robot_v18_xm430.scad`(형상).
- **버전 폴더**: v1~v10(JS 시뮬 발전) → **v11_small**(현 실물 파라미터) → v12_pybullet → v13_freedof →
  **v14_mujoco**(MuJoCo 검증 본진) → v15/16/17_fwe(FWE 코드) → **v18_optimized**(최신, mujoco/ 포함).
- **`deploy/`** — 실측→게인→ESP32 내보내기 웹툴(model.js, lqr.js, I·ℓ 독립입력).

---

## 9. 규약·단위·함정 체크리스트
- 각도: α,θ 연직기준 **오른쪽 +**, δ=θ−α, τ→[0,−1,+1], `τ=−Kx`. **β=(p1α+p2θ)/(Mt·h), x_CoM=Rφ+hβ.**
- **LC1(=ℓ1)은 발 기준, LC2(=ℓ2)는 힙 기준.** I1,I2는 자기 CoM 기준(복합진자 I_cm=I_pivot−m·d²).
- **분리가정 금지**(3~4배 오차). 결합 2×2 A₂=M₂₂⁻¹G₂₂ 사용. 옛 메인식 (1+ε)cosωT=εcoshλT 인용 금지.
- ε 4종 위계: ε*(정지) ⊂ ε*_gen(비정지) ⊂ ε*_ft(유한시간 γ) ⊂ ε_FWE(완전사이클). **μ는 ε*·γ에 안 들어감**(δ_f*만).
- MuJoCo φ 부호 반전(−Rφ). 엔코더↔δ 부호 펌웨어 불일치(미해결).
- 전류제어 `unit=τ×285.08`, TAU_MAX=3.0(=855unit, 1193 이내), 연속 2.0~2.5 권장. DT=0.002(500Hz).
- 파라미터 스케일 3종(사람/형상목표80cm/실물45cm) 섞지 말 것. 사람스케일 p1·p2는 유효값(명목과 다름).

---

## 10. 작업 인계 (최신 기록부터; 새 작업 시 한 줄 추가, 상세는 `docs/..._작업기록_YYYYMMDD.md`)
- **2026-09-02**: ★v22 로거·측정 앱 신설 `v22_logger/` (v21_pres 사본 + 측정 앱, 포트 8220, 발표 화면은 `/pres` 보존). OpenCR USB 시리얼을 서버가 직접 읽어 3D 디지털 트윈·두 평면·원본값·스트립차트를 실시간 구동, 자동 4파일 기록, 명령 팔레트(`commands.json`), 재생·스크럽, 파이프라인 탭(원시 열에서 α·β·속도·Â·예측점을 앱이 다시 계산, 펌웨어 열과 Δ 대조), 분석 도구 9종(P2R·λ·시행·φ_eq·감쇠진동·놓기경계·동정…)이 구간·점·식·잔차를 그대로 보여줌. 0822 데이터로 P2R 0.42855±0.00105(문서 64/70 일치)·λ+5.39/λ−5.81(문서 5.33/5.54) 재현. tests_v22·e2e_v22·tests_v21 ALL PASS. 노트 `docs/v22_로거_측정앱_설계노트_20260902.md`, `v22_logger/README_v22.md`. 다음: 원시값만 찍는 새 펌웨어 + 실물 브링업.
- **2026-07-05**: 예측변위(q+q̇/λ) 정확성 검토 — 선형모델에서 근사가 아니라 정확한 모드 대수(u₂ᵀq_pred/2 = 성장지수 e^{λt}의 계수 A)임을 확인. 임의 4D 상태서 ε*_gen 접기 후 |A|~10⁻¹⁷, 속도무시 대조군은 0.7~1.1s 낙하(기존 MuJoCo 관찰과 정합). 실오차원 4종: 소각근사(±2% 기검증)·상태추정 계통편향(β̇ 1°/s→여유 1.2s)·프로파일 충실도·잔여진동(유계≠소진폭). 노트 `docs/예측변위_정확성_검토_20260705.md`, 코드 `analysis/pred_disp_check.py`. 후속: (φ,β) 그림은 사영(그림자)이며 원위치 점으론 안정 판정 불가 — 대신 **예측점 q_pred=q+q̇/λ를 같은 평면에 찍으면 안정모드 직선이 정확한 판정선**(u₂⊥v₁이라 직선이탈거리=2A/|u₂|; A=0이면 직선 위 미끄러짐, 노트 §5, 그림 `docs/예측점평면_궤적_R0.7.png`, 코드 `analysis/pred_plane_traj.py`). 후속2: 예측점 평면 시나리오 5종(순간 단일접기·순간 완전사이클·유한시간 접기·유한시간 완전사이클·반복) 그림+해설 docx 작성 — `docs/예측점평면_FWE_상태공간_시나리오_20260705.docx`, 코드 `analysis/pred_plane_scenarios.py`. γ=1.531(=docs "+53%") 재확인, ε_FWE 닫힌형=수치 0.00%. ★신규 발견: 접기 실행오차 ρ는 e^{λ(T_w+T_rest)}배 증폭 → 반복 유계조건 **루프이득 ρ·e^{λ(T_w+T_rest)}<1** (같은 +5%가 T_w=0.3T_osc면 유계, T_w=T_osc면 발산). T_w=n·T_osc(잔여진동 최소) vs 짧은 T_w(오차 강건, 잔여계수 최소는 λT_w=ln2) 트레이드오프 — 유도·검증 후속 과제.
- **2026-07-02**: ★θ폭주 코드원인 확정 — OpenCR `imu.gyroData[]`는 deg/s가 아니라 raw LSB(0.061dps/LSB)라
  전 펌웨어의 θ̇가 **16.4배 과대**(thAcc는 atan2라 무사). 또 57600 baud에선 500Hz 불가(실제 ~70Hz).
  **v2 신규**(`opencr_test/lqr_balance_current_v2/` + `compute_K_measured_v2.py`, v1 보존): imu.gy(dps) 사용,
  1Mbps 자동승격 + vel·pos 8바이트 단일 read + 2ms 페이싱, δ·δ̇에 MOTOR_DIR 적용, 캘리브 200샘플 평균,
  칼만 z₀를 선형가속 보상 측정모델 z₀=θ−ẍ_imu/g (C0_acc + D0_FEED·τ, **ELL_IMU ★실측 필요**)로 확장
  (1g 게이팅은 안전장치로 유지). 사이드 시뮬: rank6, 관측기 0.9985, θ 추정오차 <1°, 폭주 없음.
  노트 `docs/펌웨어v2_자이로단위_버스타이밍_수정노트_20260702.md`. ⚠`fwe_demo_standalone` 등도 같은 자이로 단위 버그 보유.
- **2026-06-22**: θ폭주·비전류제어 두 병목 동시해결. 신규 펌웨어 `opencr_test/lqr_balance_current/`
  — `OP_CURRENT`로 τ 직접명령 + CF 제거하고 물리측정(θ_acc 게이팅·θ̇_gyro·δ엔코더·δ̇=PRESENT_VELOCITY)을
  단일 모델기반 칼만에 직접 투입(α·φ는 모델 커플링으로 복원). `compute_K_measured.py`의 칼만 C를 물리측정
  4×6으로 교체(L 재계산). 사이드 시뮬 검증: 관측 rank6·관측기 안정(0.9937), 추정오차 0.0004°, 무노이즈 θ→0(폭주 없음).
  설계노트 `docs/전류제어_단일칼만_설계노트_20260622.md`. **실물 브링업(MOTOR_DIR·엔코더부호·전류상한) 대기.**
- **2026-06-21**: 유한시간 펴기 일반화 — 마스터 전파법칙(δ경계 일반화)으로 접기/펴기 비대칭 규명(킥 부호 반대·시간이자 e^{λT}는 동방향), 유한시간 완전사이클 δ_f* 닫힌형 유도+수치 정확검증(닫힌형=수치 0.000%), (φ,β) 상태공간 궤적 시각화. 노트 `유한시간_펴기와_상태공간궤적_유도노트.docx`, 코드 `analysis/fwe_finite_state_space.py`. 또: CLAUDE.md에 Paoletti 계보(§1.5)·연구서사(§1.6)·줄 V자기하/피타고라스(§2.0)·δ 부호 해석(§2.1) 교정 반영.
- **2026-06-20**: 실측 캘리브레이션 절차서 + 전류제어 토크보정 스케치(`torque_cal`) + 실측 K재계산
  (`compute_K_measured.py`) 작성. 펌웨어가 전류제어 아님(위치+KP_EQUIV) 발견, θ추정 폭주 진단
  (자기반작용 양의피드백 + 전류글리치 + CF/칼만 이중필터). 본 CLAUDE.md 생성(최신 docx 4종 반영).
- **2026-06-12**: 완전사이클(과접기-대기-펴기) ε_FWE 닫힌형, T_w=n·T_osc (`펴기효과와_FWE완전사이클_v2.docx`).
- **2026-06-10**: 결합모드 완결본 + 유한시간 γ 보정 docx. ε*·ε*_gen·ε*_ft 정립. 전람회 보고서 초안.
- **2026-06-15**: 형상최적화(30:70, h≈43cm, 장비 머리쪽).
- **2026-06-10(소형)**: 소형 MuJoCo ε* 경향성, PD 서보게인 튜닝(ωn=200, ζ=0.7).
